# Flow attack framework

## TODOS
* <s>Add filtering on placement of projected patch</s>
** <s>Place patch only fully inside road mask and inside the image boundaries</s>
** <s>Place patch inside the biggest continious road area</s>
* <s>Add elipsoid shape for a patch instead of a circle</s>
* Add masking inside the loss based on the real person outline proportions from the dataset, make the mask smaller
* Add ability to disable ss or mde models so both wouldnt run constantly
* Install CARLA